spring:
  application:
    name: yu-ai-agent
  profiles:
    active: local
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY:${API-KEY:}}
      chat:
        options:
          model: qwen-max
        enabled: false
      embedding:
        enabled: true
        options:
          model: text-embedding-v2
      connection-timeout: 30000
      read-timeout: 1200000
    ollama:
      base-url: http://localhost:11434
      chat:
        model: qwen3:0.6b
#    vectorstore:
#      pgvector:
#        index-type: HNSW
#        dimensions: 1536
#        distance-type: COSINE_DISTANCE
#        max-document-batch-size: 10000
    mcp:
      client:
        stdio:
          servers-configuration: classpath:mcp-server.json
  datasource:
    url: jdbc:postgresql://rm-cn-j8g4s92jp0001q8o.rwlb.rds.aliyuncs.com:5432/traver-ai-agent
    username: my_user
    password: 123456Chen!
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      database: 1
      host: 139.129.36.181
      port: 6379
      password: Redis@StrongPass2025
      timeout: 5000
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms
# ... existing code ...

chat:
  memory:
    redis:
      ttl: 2592000
      max-history: 100


server:
  port: 8123
  servlet:
    context-path: /api

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: default
      paths-to-match: /**
      packages-to-scan: com.cxy.yuaiagent.controller

knife4j:
  enable: true
  setting:
    language: zh_cn

# searchApi
search-api:
  api-key: vh8UaQz6M8vz1F7QUGVihp7k


travel:
  rag:
    init-documents: false
    init-batch-size: 25
  mcp:
    required-keys:
      pexels: ${PEXELS_API_KEY:}

aliyun:
  oss:
    endpoint: ${ALIYUN_OSS_ENDPOINT:https://oss-cn-beijing.aliyuncs.com}
    bucketName: ${ALIYUN_OSS_BUCKET:java-cxy}
    region: ${ALIYUN_OSS_REGION:cn-beijing}
